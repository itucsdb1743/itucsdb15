class Bug:
        def __init__(self, bugid, bugcause, userid, focus, fixed):
            self.bugid = bugid
            self.bugcause = bugcause
            self.userid = userid
            self.focus = focus
            self.fixed = fixed